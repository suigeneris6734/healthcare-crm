import { Link } from 'react-router-dom';import StatusBadge from './StatusBadge';import { typeLabels } from '../utils/labels';
export default function OrganizationTable({rows=[],onEdit,onDelete}){if(!rows.length)return <div className="card text-slate-500">Kayıt bulunamadı. Filtreleri değiştirin veya yeni kurum ekleyin.</div>; return <div className="card overflow-x-auto"><table className="org-table text-sm"><thead><tr><th className="org-name-col">Kurum</th><th>Tür</th><th>Şehir</th><th>E-posta</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>{rows.map(o=><tr key={o.id} className="align-middle"><td className="org-name-cell"><Link title={o.name} className="org-name-link" to={`/organizations/${o.id}`}>{o.name}</Link></td><td className="whitespace-nowrap">{typeLabels[o.type]||o.type}</td><td className="max-w-40 truncate" title={o.city||''}>{o.city}</td><td className="org-email-cell" title={o.email||''}>{o.email}</td><td className="whitespace-nowrap"><StatusBadge status={o.status}/></td><td className="org-actions"><Link className="btn-light" to={`/organizations/${o.id}`}>Detay</Link><button className="btn-light" onClick={()=>onEdit(o)}>Düzenle</button><button className="btn-light text-red-700" onClick={()=>onDelete(o)}>Sil</button></td></tr>)}</tbody></table></div>}
