import {useEffect,useState} from 'react';import {api} from '../services/api';import ComposeModal from '../components/ComposeModal';
export default function FollowUpsPage(){const[rows,setRows]=useState([]),[compose,setCompose]=useState(null),[msg,setMsg]=useState('');async function load(){setRows(await api.get('/followups/due'))}useEffect(()=>{load()},[]);return <div className="space-y-5"><h1 className="text-2xl font-bold">Takipler</h1><p className="text-slate-500">İlk mailden sonra 15 günü dolan kurumlar burada görünür.</p><button className="btn" onClick={async()=>{await api.post('/followups/refresh');setMsg('Takipler yenilendi');load()}}>Takipleri Yenile</button>{msg&&<p className="card text-emerald-700">{msg}</p>}{!rows.length?<div className="card text-slate-500">Şu anda takip zamanı gelen kurum yok.</div>:<div className="card overflow-x-auto"><table className="w-full text-sm"><thead><tr><th>Kurum</th><th>Şehir</th><th>E-posta</th><th>İlk Mailden Beri</th><th>İşlem</th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td>{o.name}</td><td>{o.city}</td><td>{o.email}</td><td>{o.days_since_first_email} gün</td><td><button className="btn-light" onClick={()=>setCompose({organization:o})}>Takip Maili Hazırla</button></td></tr>)}</tbody></table></div>}<ComposeModal open={!!compose} organization={compose?.organization} actionType="FOLLOW_UP" onClose={()=>setCompose(null)} onSent={()=>{setMsg('Takip maili gönderildi');load()}}/></div>}
