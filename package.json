{
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "sh -c \"npm --prefix backend start & npm --prefix frontend run dev\"",
    "seed": "npm --prefix backend run seed",
    "build": "npm --prefix frontend run build"
  },
  "private": true
}
